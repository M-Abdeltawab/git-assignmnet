# git-assignmnet

#For the 1st question I have created the repo from github website then i connected it my making a new directory which i called Assignment5 then i used git clone to get a copy from the repo to my ubuntu version in the mentioned directory.

#For the 2nd question i created a new file by touch command i nammed it .gitignore and then i edited the file by vim text editor to include everything i would like to ignore like the .tmp files and .log etc..

#For the 3rd question i used  git checkout -b followed by the new branch name to create the new branch and switch it in the same time. I created the 3 new file and edited them by vim to make them include text. To stage i used git add command ,to commit i used git commit command followed by m 'my comment for what i did in this commit' and to push it to github i used git push followed by my remote (origin) followed by the branch main i want to add (main).

#for the 4th question i used git checkout -b for the 3 branches and edited the text files. 

#for the 5th question it is explained in detail by screenshots in the provided document on the classroom. 

#for the 6th question i created the file and then typied the bash command with wc --word and assigned a variable to store the value to and then print it as an output by echo command.

#for the 7th question we created the branch and switched to it then added the path of the directory. 

#for the 8th question it is explainted in document attached on classroom.

#for the 9th question we created the branch and then merged the text-files and script branch by git merge . 

#for the 10th question it is expplained by screenshots in the mentioned document.

Regards, 
Mahmoud. 
